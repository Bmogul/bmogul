import 'package:flutter/material.dart';

// Primary Accent Colors
const kPortfolioBlue = Color(0xFF7EB8DA);        // Soft cyan-blue - primary accent
const kPortfolioPurple = Color(0xFFBE9DDF);      // Lavender purple - secondary accent
const kPortfolioPink = Color(0xFFFFA5D8);        // Soft pink - highlights & special elements
const kPortfolioGreen = Color(0xFF98BFAD);       // Muted sage green - secondary text & paths

// Dark Mode Colors
const kPortfolioDarkBg = Color(0xFF0A0E14);      // Primary background (darkest)
const kPortfolioDarkBg2 = Color(0xFF1A1E24);     // Secondary background (cards, sidebar)
const kPortfolioDarkBg3 = Color(0xFF252A31);     // Tertiary background (hover states)
const kPortfolioDarkBorder = Color(0xFF3A3F46);  // Border color

const kPortfolioDarkTextPrimary = Color(0xFFFFFFFF);    // Primary text (white)
const kPortfolioDarkTextSecondary = Color(0xFF98BFAD);  // Secondary text (green)

const kPortfolioDarkHover = Color(0xFF1E3A4A);   // Hover background color

// Light Mode Colors
const kPortfolioLightBg = Color(0xFFF5F7FA);     // Primary background
const kPortfolioLightBg2 = Color(0xFFE8ECF1);    // Secondary background
const kPortfolioLightBg3 = Color(0xFFD5DCE5);    // Tertiary background
const kPortfolioLightBorder = Color(0xFFB8C2CC); // Border color

const kPortfolioLightTextPrimary = Color(0xFF2C3E50);   // Primary text (dark blue-gray)
const kPortfolioLightTextSecondary = Color(0xFF5A8270); // Secondary text (darker green)

const kPortfolioLightHover = Color(0xFFC9DAE8);  // Hover background color

// Light Mode Accent Variants (adjusted for better contrast)
const kPortfolioBlueLM = Color(0xFF5A9AB8);      // Light mode blue
const kPortfolioPurpleLM = Color(0xFF9C7DBD);    // Light mode purple
const kPortfolioPinkLM = Color(0xFFD683B6);      // Light mode pink

// Gradient Colors
const kPortfolioGradientStart = kPortfolioBlue;
const kPortfolioGradientMiddle = kPortfolioPurple;
const kPortfolioGradientEnd = kPortfolioPink;

// Status Colors
const kPortfolioSuccess = Color(0xFF4CAF50);     // Success state
const kPortfolioWarning = Color(0xFFFF9800);     // Warning state
const kPortfolioError = Color(0xFFF44336);       // Error state
const kPortfolioInfo = kPortfolioBlue;           // Info state

// Special Use Colors
const kPortfolioCodeBg = Color(0xFF161B22);      // Code block background
const kPortfolioSelection = Color(0xFF264F78);   // Text selection
const kPortfolioFocus = kPortfolioBlue;          // Focus indicator

// Chart & Visualization Colors
const kPortfolioChart1 = kPortfolioBlue;
const kPortfolioChart2 = kPortfolioPurple;
const kPortfolioChart3 = kPortfolioPink;
const kPortfolioChart4 = kPortfolioGreen;
const kPortfolioChart5 = Color(0xFF84C9FB);

// Shadow Colors
const kPortfolioShadowDark = Color(0x40000000);   // 25% black
const kPortfolioShadowLight = Color(0x1A000000);  // 10% black
const kPortfolioGlowBlue = Color(0x4D7EB8DA);     // 30% blue glow
